#Pandas
#Pandas는 python 머신러닝에서 가장 많이 쓰이는 데이터 처리 라이브러리이다.
#앞으로 머신러닝에서 다루는 대부분의 데이터는 행과 열로 되어있는 2차원 데이터들을 다루게 될것이다.
#판다스는 이러한 2차원 데이터를 가공(processing)하는데 효율적인 기능들을 제공한다.
# https://pandas.pydata.org/
import pandas as pd

year = [2014,2018,2020,2021]

name = ["철수","영희","재석","명수"]

list2_series = pd.Series(data=year,index=name)
